#pragma once
// Phase 1: Editor is handled by returning juce::GenericAudioProcessorEditor
// directly from XYZPanProcessor::createEditor(). No custom editor class needed.
// Phase 6 will replace this with a custom OpenGL editor.
