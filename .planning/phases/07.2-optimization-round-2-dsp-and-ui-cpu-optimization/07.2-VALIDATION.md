---
phase: 07.2
slug: optimization-round-2-dsp-and-ui-cpu-optimization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-17
---

# Phase 07.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 3.7.1 |
| **Config file** | none — discovered via `catch_discover_tests(XYZPanTests)` in tests/CMakeLists.txt |
| **Quick run command** | `ctest --test-dir build -R XYZPanTests -C Release` |
| **Full suite command** | `ctest --test-dir build -C Release` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `ctest --test-dir build -R XYZPanTests -C Release`
- **After every plan wave:** Run `ctest --test-dir build -C Release`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.2-01-01 | 01 | 1 | INFRA-03 | unit | `ctest --test-dir build -R "SineLUT" -C Release` | ❌ W0 | ⬜ pending |
| 07.2-01-02 | 01 | 1 | INFRA-03 | regression | `ctest --test-dir build -R "LFO" -C Release` | ✅ | ⬜ pending |
| 07.2-01-03 | 01 | 1 | INFRA-03 | performance | `ctest --test-dir build -R "Engine throughput" -C Release` | ✅ | ⬜ pending |
| 07.2-02-01 | 02 | 1 | INFRA-03 | regression | `ctest --test-dir build -R XYZPanTests -C Release` | ✅ | ⬜ pending |
| 07.2-02-02 | 02 | 1 | INFRA-03 | performance | `ctest --test-dir build -R "Engine throughput" -C Release` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/engine/TestSineLUT.cpp` — stubs for SineLUT accuracy (max error < 0.001), wrapping correctness at boundaries, cosine via phase offset
- [ ] Extend `tests/engine/TestPerformance.cpp` — add worst-case benchmark: stereo + all 6 LFOs + all 3 orbit planes active

*Existing engine tests cover audio correctness regressions. Existing performance tests cover mono, 64-sample, and stereo throughput.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GL view reduces CPU when idle | INFRA-03 | Requires visual observation + Task Manager | Open plugin UI, observe CPU in Task Manager with and without parameter movement |
| GL view updates within 33ms of automation | INFRA-03 | Requires DAW automation playback | Play automation curve, observe GL view tracks position smoothly |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
