---
phase: 07.5
slug: vst-ui-design-overhaul
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-20
---

# Phase 07.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 (via CMake FetchContent) |
| **Config file** | `tests/CMakeLists.txt` |
| **Quick run command** | `ctest --test-dir build -R "XYZPanPluginTests" --output-on-failure` |
| **Full suite command** | `ctest --test-dir build --output-on-failure` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `ctest --test-dir build -R "XYZPanPluginTests" --output-on-failure`
- **After every plan wave:** Run `ctest --test-dir build --output-on-failure`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.5-01-01 | 01 | 1 | UI-OVH-01 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-01-02 | 01 | 1 | UI-OVH-02 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-01-03 | 01 | 1 | UI-OVH-06 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-01-04 | 01 | 1 | UI-OVH-08 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-02-01 | 02 | 1 | UI-OVH-03 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-02-02 | 02 | 1 | UI-OVH-04 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-02-03 | 02 | 1 | UI-OVH-05 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |
| 07.5-03-01 | 03 | 2 | UI-OVH-07 | regression | `ctest --test-dir build --output-on-failure` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files needed — UI layout changes are verified via build + regression + manual visual inspection.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Default window size is 1100×750 | UI-OVH-01 | Visual layout verification | Open plugin standalone, verify window proportions are landscape |
| Multi-instance LookAndFeel safety | UI-OVH-02 | Requires DAW with two instances | Load two instances in DAW, verify both render correctly |
| ToggleButton renders with Alchemy theme | UI-OVH-03 | Visual appearance check | Click "Always Face Observer" toggle, verify gold border when active |
| ComboBox renders with bronze border | UI-OVH-04 | Visual appearance check | Click preset dropdown, verify bronze border and gold arrow |
| Popup menu items themed | UI-OVH-05 | Visual appearance check | Open preset dropdown, verify dark background with gold text |
| LFO strips render correctly at 750px | UI-OVH-08 | Visual layout check | Verify all 6 LFO strips show shape selector, waveform, knobs, sync button |
| Snap button shows active state | UI-OVH-07 | Visual state check | Click XY/XZ/YZ buttons, verify gold border on active snap |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
