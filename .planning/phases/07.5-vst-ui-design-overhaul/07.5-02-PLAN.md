---
phase: 07.5-vst-ui-design-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/AlchemyLookAndFeel.h
  - ui/AlchemyLookAndFeel.cpp
autonomous: true
requirements:
  - UI-OVH-03
  - UI-OVH-04
  - UI-OVH-05
must_haves:
  truths:
    - "ToggleButtons render as Alchemy-themed buttons with gold border when ON and bronze border when OFF"
    - "ComboBox renders with dark iron background, bronze border, and gold dropdown arrow"
    - "PopupMenu items render with dark iron background, parchment text, and bronze highlight"
  artifacts:
    - path: "ui/AlchemyLookAndFeel.h"
      provides: "drawToggleButton, drawComboBox, drawPopupMenuItem declarations"
      contains: "drawToggleButton"
    - path: "ui/AlchemyLookAndFeel.cpp"
      provides: "Full implementations of all three overrides"
      contains: "drawPopupMenuItem"
  key_links:
    - from: "ui/AlchemyLookAndFeel.cpp drawToggleButton()"
      to: "ToggleButton rendering"
      via: "LookAndFeel virtual dispatch"
      pattern: "drawToggleButton"
    - from: "ui/AlchemyLookAndFeel.cpp drawComboBox()"
      to: "ComboBox rendering"
      via: "LookAndFeel virtual dispatch"
      pattern: "drawComboBox"
---

<objective>
Add three missing LookAndFeel overrides to AlchemyLookAndFeel: drawToggleButton (for faceListenerToggle_
and orbitTempoSyncToggle_), drawComboBox (for presetCombo_), and drawPopupMenuItem (for the preset
dropdown popup list). These controls currently render with JUCE LookAndFeel_V4 defaults which clash
with the Alchemy dark/gold theme.

Purpose: Visual consistency -- all interactive controls should match the Alchemy palette. ToggleButtons
currently show a V4-default checkbox tick mark. ComboBox and PopupMenu use default V4 border/arrow
geometry that does not match the bronze/gold scheme.

Output: AlchemyLookAndFeel.h/.cpp with three new virtual overrides fully implemented.
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.5-vst-ui-design-overhaul/07.5-RESEARCH.md

<interfaces>
From ui/AlchemyLookAndFeel.h:
```cpp
static constexpr uint32_t kBackground  = 0xFF110B1Au;
static constexpr uint32_t kDarkIron    = 0xFF1C1228u;
static constexpr uint32_t kBronze      = 0xFF7B5EA7u;
static constexpr uint32_t kWarmGold    = 0xFFD4A843u;
static constexpr uint32_t kParchment   = 0xFFC8BCD8u;
static constexpr uint32_t kBrightGold  = 0xFFFFD700u;
static constexpr uint32_t kHoverGold   = 0xFFFFE566u;
```

From ui/AlchemyLookAndFeel.h (existing overrides):
```cpp
void drawRotarySlider(...) override;
void drawButtonBackground(...) override;
void drawButtonText(...) override;
void drawLinearSlider(...) override;
void drawLabel(...) override;
```

From ui/AlchemyLookAndFeel.cpp constructor:
```cpp
setColour(juce::ComboBox::backgroundColourId, juce::Colour(kDarkIron));
setColour(juce::ComboBox::textColourId,       juce::Colour(kParchment));
setColour(juce::PopupMenu::backgroundColourId, juce::Colour(kDarkIron));
setColour(juce::PopupMenu::textColourId,       juce::Colour(kParchment));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drawToggleButton override to AlchemyLookAndFeel</name>
  <files>ui/AlchemyLookAndFeel.h, ui/AlchemyLookAndFeel.cpp</files>
  <read_first>
    ui/AlchemyLookAndFeel.h
    ui/AlchemyLookAndFeel.cpp
    .planning/phases/07.5-vst-ui-design-overhaul/07.5-RESEARCH.md
  </read_first>
  <action>
**In AlchemyLookAndFeel.h**, add this declaration in the public section (after `drawLabel`):

```cpp
void drawToggleButton(juce::Graphics& g, juce::ToggleButton& button,
                      bool shouldDrawButtonAsHighlighted,
                      bool shouldDrawButtonAsDown) override;
```

**In AlchemyLookAndFeel.cpp**, add this implementation:

```cpp
void AlchemyLookAndFeel::drawToggleButton(
    juce::Graphics& g, juce::ToggleButton& button,
    bool shouldDrawButtonAsHighlighted,
    bool shouldDrawButtonAsDown)
{
    auto bounds = button.getLocalBounds().toFloat().reduced(0.5f);

    juce::Colour fill = juce::Colour(kDarkIron);
    if (shouldDrawButtonAsDown)
        fill = fill.brighter(0.2f);
    else if (shouldDrawButtonAsHighlighted)
        fill = fill.brighter(0.1f);
    g.setColour(fill);
    g.fillRoundedRectangle(bounds, 3.0f);

    const juce::Colour border = button.getToggleState()
        ? juce::Colour(kWarmGold)
        : juce::Colour(kBronze);
    g.setColour(border);
    g.drawRoundedRectangle(bounds, 3.0f, 1.5f);

    const auto fontSize = juce::jmin(12.0f,
        static_cast<float>(button.getHeight()) * 0.5f);
    g.setFont(juce::Font(juce::FontOptions(fontSize)));
    g.setColour(button.getToggleState()
        ? juce::Colour(kBrightGold)
        : juce::Colour(kParchment).withMultipliedAlpha(
            button.isEnabled() ? 1.0f : 0.5f));
    g.drawText(button.getButtonText(), button.getLocalBounds(),
               juce::Justification::centred, true);
}
```

This renders ToggleButtons as text-only buttons (no tick box). Toggle state shown via border color (gold=on, bronze=off) and text color (bright gold=on, parchment=off).
  </action>
  <verify>
    <automated>cd "C:/Users/Luke/Desktop/programming/xyzpan" && cmake --build build --config Release --target XYZPan 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/AlchemyLookAndFeel.h contains `void drawToggleButton(juce::Graphics& g, juce::ToggleButton& button,`
    - ui/AlchemyLookAndFeel.cpp contains `AlchemyLookAndFeel::drawToggleButton(`
    - ui/AlchemyLookAndFeel.cpp drawToggleButton contains `kWarmGold` and `kBronze` and `kBrightGold` and `kParchment`
    - ui/AlchemyLookAndFeel.cpp drawToggleButton does NOT contain `drawTickBox` or `tick`
    - Build succeeds
  </acceptance_criteria>
  <done>
    ToggleButtons render as Alchemy-themed text buttons with gold border ON / bronze border OFF.
    No JUCE default checkbox tick mark.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add drawComboBox and drawPopupMenuItem overrides to AlchemyLookAndFeel</name>
  <files>ui/AlchemyLookAndFeel.h, ui/AlchemyLookAndFeel.cpp</files>
  <read_first>
    ui/AlchemyLookAndFeel.h
    ui/AlchemyLookAndFeel.cpp
  </read_first>
  <action>
**In AlchemyLookAndFeel.h**, add these declarations after drawToggleButton:

```cpp
void drawComboBox(juce::Graphics& g, int width, int height,
                  bool isButtonDown,
                  int buttonX, int buttonY, int buttonW, int buttonH,
                  juce::ComboBox& box) override;

void drawPopupMenuItem(juce::Graphics& g,
                       const juce::Rectangle<int>& area,
                       bool isSeparator, bool isActive,
                       bool isHighlighted, bool isTicked, bool hasSubMenu,
                       const juce::String& text,
                       const juce::String& shortcutKeyText,
                       const juce::Drawable* icon,
                       const juce::Colour* textColour) override;
```

**In AlchemyLookAndFeel.cpp**, add drawComboBox:

```cpp
void AlchemyLookAndFeel::drawComboBox(
    juce::Graphics& g, int width, int height,
    bool isButtonDown,
    int /*buttonX*/, int /*buttonY*/, int /*buttonW*/, int /*buttonH*/,
    juce::ComboBox& box)
{
    const auto bounds = juce::Rectangle<float>(0.0f, 0.0f,
        static_cast<float>(width), static_cast<float>(height));

    g.setColour(juce::Colour(kDarkIron));
    g.fillRoundedRectangle(bounds, 3.0f);

    g.setColour(isButtonDown ? juce::Colour(kWarmGold) : juce::Colour(kBronze));
    g.drawRoundedRectangle(bounds.reduced(0.5f), 3.0f, 1.0f);

    {
        const float arrowH = static_cast<float>(height) * 0.3f;
        const float arrowW = arrowH * 1.2f;
        const float arrowX = static_cast<float>(width) - arrowW - 8.0f;
        const float arrowY = (static_cast<float>(height) - arrowH) * 0.5f;

        juce::Path arrow;
        arrow.addTriangle(arrowX, arrowY,
                          arrowX + arrowW, arrowY,
                          arrowX + arrowW * 0.5f, arrowY + arrowH);
        g.setColour(juce::Colour(kWarmGold).withAlpha(box.isEnabled() ? 1.0f : 0.4f));
        g.fillPath(arrow);
    }
}
```

And drawPopupMenuItem:

```cpp
void AlchemyLookAndFeel::drawPopupMenuItem(
    juce::Graphics& g,
    const juce::Rectangle<int>& area,
    bool isSeparator, bool isActive,
    bool isHighlighted, bool isTicked, bool /*hasSubMenu*/,
    const juce::String& text,
    const juce::String& /*shortcutKeyText*/,
    const juce::Drawable* /*icon*/,
    const juce::Colour* /*textColour*/)
{
    if (isSeparator)
    {
        g.setColour(juce::Colour(kBronze).withAlpha(0.4f));
        g.fillRect(area.reduced(5, 0).removeFromTop(1));
        return;
    }

    if (isHighlighted && isActive)
    {
        g.setColour(juce::Colour(kBronze).withAlpha(0.25f));
        g.fillRect(area);
    }

    g.setFont(juce::Font(juce::FontOptions(
        juce::jmin(14.0f, static_cast<float>(area.getHeight()) * 0.7f))));

    juce::Colour textCol = isActive
        ? juce::Colour(kParchment) : juce::Colour(kParchment).withAlpha(0.4f);
    if (isHighlighted && isActive) textCol = juce::Colour(kBrightGold);
    if (isTicked) textCol = juce::Colour(kWarmGold);

    g.setColour(textCol);
    g.drawText(text, area.reduced(12, 0), juce::Justification::centredLeft, true);

    if (isTicked)
    {
        const float dotR = 3.0f;
        const float dotX = static_cast<float>(area.getX()) + 5.0f;
        const float dotY = static_cast<float>(area.getCentreY());
        g.setColour(juce::Colour(kWarmGold));
        g.fillEllipse(dotX - dotR, dotY - dotR, dotR * 2.0f, dotR * 2.0f);
    }
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Luke/Desktop/programming/xyzpan" && cmake --build build --config Release --target XYZPan 2>&1 | tail -5 && ctest --test-dir build --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ui/AlchemyLookAndFeel.h contains `void drawComboBox(`
    - ui/AlchemyLookAndFeel.h contains `void drawPopupMenuItem(`
    - ui/AlchemyLookAndFeel.cpp contains `AlchemyLookAndFeel::drawComboBox(`
    - ui/AlchemyLookAndFeel.cpp contains `AlchemyLookAndFeel::drawPopupMenuItem(`
    - ui/AlchemyLookAndFeel.cpp drawComboBox contains `addTriangle`
    - ui/AlchemyLookAndFeel.cpp drawPopupMenuItem contains `kBrightGold` and `kWarmGold`
    - All existing tests pass
    - Build succeeds
  </acceptance_criteria>
  <done>
    ComboBox renders with Alchemy theme (dark iron bg, bronze border, gold arrow).
    PopupMenu items render with Alchemy theme (dark bg, parchment text, gold highlight,
    gold dot for ticked/selected item). All tests green.
  </done>
</task>

</tasks>

<verification>
1. Build: `cmake --build build --config Release --target XYZPan` -- zero errors
2. Tests: `ctest --test-dir build --output-on-failure` -- all pass
3. grep: `drawToggleButton` found in both .h and .cpp
4. grep: `drawComboBox` found in both .h and .cpp
5. grep: `drawPopupMenuItem` found in both .h and .cpp
</verification>

<success_criteria>
- Plugin builds successfully
- All existing tests pass
- Three new LookAndFeel overrides compile and link
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-vst-ui-design-overhaul/07.5-02-SUMMARY.md`
</output>
