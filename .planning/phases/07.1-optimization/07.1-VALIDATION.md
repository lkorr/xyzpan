---
phase: 07.1
slug: optimization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-14
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 v3 |
| **Config file** | tests/CMakeLists.txt |
| **Quick run command** | `ctest --test-dir build -R "Engine" --output-on-failure -j4` |
| **Full suite command** | `ctest --test-dir build --output-on-failure -j4` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `ctest --test-dir build -R "Engine" --output-on-failure -j4`
- **After every plan wave:** Run `ctest --test-dir build --output-on-failure -j4`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.1-01-01 | 01 | 1 | INFRA-03 | unit | `ctest --test-dir build -R "BinauralPanning\|DepthAndElevation\|DistanceProcessing\|CreativeTools" --output-on-failure` | ✅ | ⬜ pending |
| 07.1-01-02 | 01 | 1 | INFRA-03 | regression | `ctest --test-dir build --output-on-failure -j4` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. The existing Catch2 test suite (68+ tests across binaural panning, depth/elevation, distance processing, creative tools, parameter layout) provides full regression coverage for the optimization refactor.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CPU usage at 64-sample buffer | INFRA-03 | Requires DAW + profiler | Load plugin in DAW at 64-sample buffer, monitor CPU meter during automation sweep |
| Audio quality preserved | INFRA-03 | Perceptual test | A/B listen pre/post optimization with reference audio |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
