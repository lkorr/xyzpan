---
phase: 07.4-presets
plan: 02
subsystem: ui
tags: [juce, preset-ui, combobox, file-chooser, apvts, xml]

# Dependency graph
requires:
  - phase: 07.4-01
    provides: "XYZPresets::kFactoryPresets[], kNumPresets, PluginProcessor program methods (setCurrentProgram, getCurrentProgram, getNumPrograms)"

provides:
  - "Preset ComboBox in PluginEditor showing all 7 factory presets"
  - "Save button: writes current APVTS state to .xml file via FileChooser"
  - "Load button: reads .xml file and applies via apvts.replaceState() on message thread"
  - "32px preset bar above the main editor layout"

affects: [human-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Preset bar: removeFromTop(kPresetBarH) before main layout carve-out"
    - "FileChooser with shared_ptr capture in launchAsync callback (avoids dangling reference)"
    - "ComboBox IDs are 1-based; convert with +1 when adding, -1 when reading"
    - "XmlElement::writeTo(file) preferred over deprecated writeToFile()"

key-files:
  created: []
  modified:
    - plugin/PluginEditor.h
    - plugin/PluginEditor.cpp

key-decisions:
  - "Preset bar uses removeFromTop(kPresetBarH=32) pattern — shifts existing layout down by 32px without changing kDefaultH"
  - "leftColTop = leftCol.getY() used in resized() to offset all left-column Y coordinates after preset bar carve-out"
  - "paint() updated to use contentY=kPresetBarH as base offset for all left-column backgrounds and headers"
  - "XmlElement::writeTo(file) used instead of deprecated writeToFile() — Rule 1 auto-fix"
  - "ComboBox deselected (setSelectedId 0) after user preset load so factory names don't show stale state"

patterns-established:
  - "Preset bar: always carve out of b before bottomRow/leftCol/glArea to maintain clean layout cascade"

requirements-completed: [PARAM-05]

# Metrics
duration: 4min
completed: 2026-03-18
---

# Phase 07.4 Plan 02: Preset UI Summary

**In-plugin preset ComboBox (7 factory presets), Save/Load file-picker buttons added to top 32px bar of PluginEditor — all running on the message thread, zero audio-thread risk**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-18T08:53:25Z
- **Completed:** 2026-03-18T08:57:46Z
- **Tasks:** 1 complete, 1 at checkpoint (human-verify)
- **Files modified:** 2

## Accomplishments
- Preset ComboBox with all 7 factory preset names from XYZPresets::kFactoryPresets
- ComboBox onChange calls proc_.setCurrentProgram() on message thread (safe, not audio thread)
- Save button: FileChooser -> apvts.copyState().createXml().writeTo(file) — writes APVTS XML to disk
- Load button: FileChooser -> parseXML -> apvts.replaceState() on message thread — applies full state
- Preset bar layout (32px top strip) fully integrated with existing resized()/paint() geometry
- All 47 existing tests pass — zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Add preset ComboBox and Save/Load buttons** - `3099d71` (feat)
2. **Task 2: Human verify preset system** - checkpoint (awaiting human verification)

## Files Created/Modified
- `plugin/PluginEditor.h` - Added `#include "Presets.h"`, `kPresetBarH` constant, `presetCombo_`, `presetSaveBtn_`, `presetLoadBtn_` members
- `plugin/PluginEditor.cpp` - Preset bar in constructor and resized(); paint() geometry updated for kPresetBarH offset

## Decisions Made
- Preset bar uses `removeFromTop(kPresetBarH=32)` as the first carve-out in resized() so all subsequent layout (leftCol, glArea, bottomRow) automatically shifts down — clean cascade, no hardcoded offsets elsewhere
- `leftColTop = leftCol.getY()` introduced in resized() to correctly anchor left-column Y coordinates after preset bar removed from top
- `paint()` uses `contentY = kPresetBarH` as the offset base for all background fills and section headers
- ComboBox deselected after user preset load so the dropdown doesn't show a stale factory preset name

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Replaced deprecated XmlElement::writeToFile() with writeTo()**
- **Found during:** Task 1 build
- **Issue:** JUCE 8 emits C4996 deprecation warning for writeToFile() — compiler warned it was removed in favor of writeTo()
- **Fix:** Changed `xml->writeToFile(file, "")` to `xml->writeTo(file)`
- **Files modified:** plugin/PluginEditor.cpp
- **Verification:** Build completes with zero warnings, zero errors
- **Committed in:** `3099d71` (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 deprecated API replacement)
**Impact on plan:** Minimal — one-line fix, strictly better API call, no behavior change.

## Issues Encountered
None — Task 1 built cleanly on first attempt after the deprecation fix.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Preset UI is complete on the code side
- Human verification required (Task 2 checkpoint): load in DAW, verify 7 presets visible, preset switching applies knob changes, Save/Load round-trip works, no crashes during rapid switching with audio playing

---
*Phase: 07.4-presets*
*Completed: 2026-03-18*
