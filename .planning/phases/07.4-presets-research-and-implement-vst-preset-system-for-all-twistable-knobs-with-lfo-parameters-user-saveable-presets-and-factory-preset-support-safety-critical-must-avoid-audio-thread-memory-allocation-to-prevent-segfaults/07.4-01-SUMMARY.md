---
phase: 07.4-presets
plan: 01
subsystem: plugin
tags: [presets, state-serialization, audio-thread-safety, factory-presets, apvts]
dependency_graph:
  requires: []
  provides: [factory-preset-system, state-round-trip-tests]
  affects: [PluginProcessor, TestPresets]
tech_stack:
  added: []
  patterns:
    - APVTS replaceState from message thread only (setCurrentProgram, setStateInformation)
    - Embedded XML string factory presets in Presets.h namespace
    - Complete XML (all ~110 params) per preset to prevent state bleed (Pitfall 4)
key_files:
  created:
    - plugin/Presets.h
  modified:
    - plugin/PluginProcessor.h
    - plugin/PluginProcessor.cpp
    - tests/plugin/TestPresets.cpp
decisions:
  - Beat div default index = 6 (kBeatDivDefaultIndex from Constants.h, label "1" = quarter note); plan comment said 5 — corrected to 6
  - Default preset verb_wet=0.3 intentionally (creative preset choice, not parameter default of 0.0)
  - All 7 preset XMLs include all ~110 parameter IDs — no partial presets (avoids state bleed)
  - kVerbDefaultWet=0.0 in Constants.h; Default preset uses 0.3 by design
metrics:
  duration: 7 min
  completed: 2026-03-18T08:50:44Z
  tasks_completed: 2
  files_changed: 4
---

# Phase 07.4 Plan 01: Factory Preset System Summary

7 factory presets with complete XML, APVTS program methods, and 9-test suite covering PARAM-04 round-trip, PARAM-05 factory loading, and INFRA-04 audio-thread safety structural guarantee.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create Presets.h and implement PluginProcessor program methods | 81f3f33 | plugin/Presets.h (created), plugin/PluginProcessor.h, plugin/PluginProcessor.cpp |
| 2 | Replace TestPresets.cpp stub with round-trip and factory preset tests | 6cfdf8b | tests/plugin/TestPresets.cpp |

## What Was Built

**plugin/Presets.h** — Factory preset definitions in `namespace XYZPresets`:
- `struct Preset { const char* name; const char* xml; }`
- 7 factory presets as `static constexpr const char*` raw XML strings, each containing all ~110 parameter IDs
- `static const Preset kFactoryPresets[]` array
- `static constexpr int kNumPresets = 7`

Factory presets:
1. **Default** — x=0, y=1, z=0, all LFOs off, verb_wet=0.3 (init state)
2. **Orbit XY** — LFO X+Y sine at 0.5Hz depth=0.8, Y phase=0.25 for circular orbit
3. **Slow Drift** — Triangle LFOs at 0.1/0.07/0.05Hz, gentle ambient movement
4. **Behind You** — y=-0.8 rear placement, verb_wet=0.4 size=0.7 decay=0.7
5. **Fly Around** — All 3 LFOs active at 1.2/0.8/0.6Hz depth=0.9/0.9/0.5
6. **Overhead** — z=0.9 elevation, verb_size=0.6
7. **Near Whisper** — Close source, verb_wet=0.1, dist_delay_max_ms=0

**PluginProcessor.h changes:**
- Added `#include "Presets.h"`
- Replaced inline stubs with declarations: `getNumPrograms()`, `getCurrentProgram()`, `setCurrentProgram(int)`, `getProgramName(int)`
- Added `int currentPresetIndex_ = 0` to private section

**PluginProcessor.cpp changes:**
- Added `#include "Presets.h"`
- Implemented 4 program methods after `setStateInformation`
- `setCurrentProgram` parses XML and calls `apvts.replaceState()` (message thread only)
- `processBlock` untouched: zero ValueTree access, zero heap allocation (INFRA-04)

**tests/plugin/TestPresets.cpp** — 9 test cases, 29 assertions:
- PARAM-04: X+LFO_X_DEPTH round-trip; Y+Z+VERB_WET round-trip
- PARAM-05: count>=7; name spot-checks; Orbit XY applies depth>0; all presets load; Default verb_wet=0.3 (state bleed check); out-of-range ignored
- INFRA-04: structural documentation test

## Verification Results

- Plugin build: zero errors (`XYZPan_SharedCode.lib` produced)
- Plugin tests build: zero errors (`XYZPanPluginTests.exe` produced)
- Preset tests: 9/9 pass, 29 assertions
- Full plugin test suite: 12/12 pass, 47 assertions
- Engine test suite: 70/77 pass (7 pre-existing failures, unchanged)
- processBlock safety: `grep replaceState|copyState|ValueTree::fromXml PluginProcessor.cpp` → only in `setStateInformation` (line 498/506) and `setCurrentProgram` (lines 540/542), never in processBlock

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug correction] Beat div default index corrected from 5 to 6**
- Found during: Task 1 (pre-implementation research)
- Issue: Plan comment said `lfo_x_beat_div` default is "index 5", but `Constants.h::kBeatDivDefaultIndex = 6` (label "1" = quarter note). Index 5 = "3/4".
- Fix: Used `value="6"` in all preset XMLs for beat_div params
- Files modified: plugin/Presets.h
- Impact: Correct quarter-note default; plan comment was wrong

No other deviations — plan executed as designed.

## Self-Check: PASSED
