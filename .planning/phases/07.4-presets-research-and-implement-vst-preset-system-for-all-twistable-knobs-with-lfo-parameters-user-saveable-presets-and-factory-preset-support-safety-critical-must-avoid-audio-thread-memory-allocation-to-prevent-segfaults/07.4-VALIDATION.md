---
phase: 07.4
slug: presets
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 07.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 (already configured) |
| **Config file** | tests/CMakeLists.txt — XYZPanPluginTests target |
| **Quick run command** | `ctest --test-dir build -R "presets" --output-on-failure` |
| **Full suite command** | `ctest --test-dir build --output-on-failure` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `ctest --test-dir build -R "presets" --output-on-failure`
- **After every plan wave:** Run `ctest --test-dir build --output-on-failure`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | PARAM-04 | unit round-trip | `ctest --test-dir build -R "presets" --output-on-failure` | Stub exists (TestPresets.cpp) — Wave 0: replace stub | ⬜ pending |
| TBD | 01 | 1 | PARAM-05 | unit | `ctest --test-dir build -R "presets" --output-on-failure` | Stub exists | ⬜ pending |
| TBD | 01 | 1 | INFRA-04 | manual / pluginval | pluginval strictness 5 | N/A — structural review | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Replace `tests/plugin/TestPresets.cpp` stub with real round-trip tests (PARAM-04, PARAM-05)

*Existing Catch2 infrastructure covers the test framework requirement.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| No audio thread memory allocation | INFRA-04 | Cannot be unit-tested; requires runtime analysis or pluginval | Run pluginval at strictness 5; load/switch presets while audio is playing; verify no crashes over 60s |
| DAW preset list shows factory presets | PARAM-05 | DAW interop requires manual testing | Open in Reaper/Ableton; verify preset names appear in DAW's built-in preset browser |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
